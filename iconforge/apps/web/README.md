# IconFull Web

This is the Next.js demo UI for ICONFULL. It runs entirely in the browser and lets users generate icon assets without uploading files.

## Running locally

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
